import Form from "../components/Form";

export default function Register() {
    const handleClick = (name: string, password: string) => {
        
    }

    return (
        <Form title='Registre-se' handleClick={handleClick}/>
    )
}